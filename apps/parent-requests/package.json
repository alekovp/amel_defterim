{
  "name": "paragon-parent-requests",
  "private": true,
  "description": "Parent fee request form and entitlement calculator — Google Apps Script web app",
  "scripts": {
    "build": "node build.js",
    "test": "node build.js && node --test",
    "push": "node build.js && clasp push"
  },
  "engines": { "node": ">=18" }
}
